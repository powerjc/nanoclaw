# Intent: Add HA Shopping List MCP to agent runner

This skill adds `ha_shopping_list` to the `mcpServers` object and `mcp__ha_shopping_list__*` to the `allowedTools` array in `container/agent-runner/src/index.ts`.
